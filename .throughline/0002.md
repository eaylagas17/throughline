---
id: 0002
title: "Composability: interop with superpowers, ponytail, and other skills"
status: parked
intent: "throughline must stay excellent STANDALONE and also merge cleanly with other installed skills so the combination is greater. 'Great alone, greater together.' It positions as the surfacing + faithful-handoff + memory layer that sits ON TOP of whatever planner / executor / code-quality skill is present, riding their reach (superpowers, ponytail ~73k star) instead of competing. This is what turns an honest lineage into an honest alliance."
decisions:
  - "Target composition depth #1 + #2 (decided 2026-07-04 with user). #1 = convention + loose surfacing: standalone core UNCHANGED; integration is documented recipes + the surface hook loosely detecting other skills' work. #2 = active delegation: Ship hands work to the best AVAILABLE specialist and degrades to doing it itself when absent. RULE OUT #3 (deep lifecycle hooks into superpowers execution events): superpowers emits no such events, so it needs changes to superpowers or fragile patching, and it breaks the zero-coupling ethos."
  - "STANDALONE-FIRST is non-negotiable. If superpowers/ponytail are absent, NOTHING changes and no errors surface. Every integration is a thin, optional, DEFENSIVE adapter, never a logic fork (mirrors throughline's existing adapter-model, design spec section 8). Reuse throughline's tolerant-parser DNA (store.mjs) for any external-format reading."
  - "superpowers Layer 1 is ALREADY FREE, only document it: anchors.plan already points at any plan file including docs/superpowers/plans/*.md, so ship / re-validate / drift-check already work against a superpowers plan. throughline's OWN core plan IS a superpowers plan (built-in self-demo)."
  - "superpowers Layer 2 (build): the surface hook loosely detects in-progress superpowers plan files (unchecked '- [ ]' steps under '### Task N' headers) and lists them beside .throughline items. This fills superpowers' exact gap: it has no session-start surfacing."
  - "superpowers Layer 3 delegation (build): Ship hands ACTUAL execution to superpowers subagent-driven-development when present; checkpoint captures the per-phase delta superpowers cannot (the real fidelity edge). throughline never reimplements the executor."
  - "ponytail: ALREADY composed in Ship step 6 ('implement minimally via the ponytail ladder'). Formalize as delegate-when-present, degrade to the inline ladder when absent. So the composability pattern is not new; this item extends it to superpowers and makes it a general doctrine."
  - "MOAT HONESTY: 'surfaces superpowers plans' alone is a weak moat (superpowers could just absorb it). throughline's defensible value is the FULL backlog + delta-capture + drift + re-validate model that ALSO works with superpowers plans. Lead positioning with that, not surfacing alone."
  - "SEQUENCING: parked as its OWN item on purpose; do NOT block publishing (item 0001 Phase 4). The cheap README add-on positioning + the Layer-1 'it already anchors to superpowers plans' doc note MAY land with the adoption work (item 0001); the CODE (Layer 2/3 + ponytail formalization) is this item, demand-gated on real dual-user demand."
  - "STYLE decision from item 0001 applies here too: no em dashes in reader/contributor-facing text."
open_questions: []  # all four resolved in the 2026-07-04 composability spec; see checkpoint
acceptance: "throughline works fully standalone with zero other skills installed (no errors, no behavior change). When superpowers is present: in-progress plans surface at session start; an item can anchor to a superpowers plan, ship (delegating execution to superpowers), and checkpoint deltas. When ponytail is present: Ship delegates minimal-code writing to it. EVERY integration degrades to standalone behavior when the other skill is absent. The README states the honest add-on positioning."
anchors:
  sha: 48a0112e6965330d240782761fb01e1023cf5aa2
  files:
    - skills/throughline-ship/SKILL.md
    - skills/throughline-capture/SKILL.md
    - hooks/throughline-surface.mjs
    - scripts/lib/render.mjs
    - scripts/lib/config.mjs
    - scripts/lib/superpowers.mjs
    - references/composing.md
    - README.md
    - docs/superpowers/specs/2026-07-04-throughline-composability-design.md
    - docs/superpowers/plans/2026-07-04-throughline-composability.md
  plan: docs/superpowers/plans/2026-07-04-throughline-composability.md
phases: []
---
Parked 2026-07-04 from a brainstorming session (superpowers:brainstorming), after shipping item 0001 Phase 3 (the benchmark). This is a design decision captured, not yet a plan.

Fresh-session order when picked up: read this item, then the design spec's adapter model (section 8) and reuse table (section 10) via the plan anchor, then decide whether to write a dedicated composability spec before implementing (see the last open_question). Layer 1 needs no code (document only); Layer 2 (surface detection) and Layer 3 (Ship/checkpoint delegation) plus the ponytail formalization are the build.

Link to the benchmark thread: Layer 3's delta-capture during executing-plans is exactly what would make throughline demonstrably beat superpowers (not just beat a prose paste-prompt). The current benchmark (item 0001 / benchmarks/) does NOT isolate that, because its throughline arm already carries the plan a superpowers session would also have. A fair 3-arm benchmark (prose / plan-only / plan+delta) on a scenario where the drift-causing fact is DISCOVERED mid-phase (absent from the up-front plan) would validate Layer 3's value. Consider building that alongside this item.

---
Checkpoint 2026-07-04 (planned; Layer 1 shipped):
- Layer 1 (README "Composes with superpowers" section) is DONE this session, not yet committed. It documents the already-free anchoring (Ship / re-validate / drift-check work against a superpowers plan file) with the honest moat framing (full model, not surfacing alone).
- A composability DESIGN SPEC and a phased IMPLEMENTATION PLAN now exist (both uncommitted):
  - spec: docs/superpowers/specs/2026-07-04-throughline-composability-design.md
  - plan: docs/superpowers/plans/2026-07-04-throughline-composability.md (5 TDD tasks)
- All four open_questions are RESOLVED in the spec (recorded here so a fresh session need not re-derive):
  1. Detection splits by layer, no install-probe: Layer 2 (hook) acts on plan files that exist; Layer 3 / ponytail (agent-run Ship) act on the agent's available-skills list.
  2. Layer 2 keys on the mixed-checkbox test (both checked and unchecked present), independent of the `### Task N` header structure, so it survives plan-format drift. Glob is config-overridable (`compose.plansGlob`).
  3. Surface UX: a separate, clearly labeled, subordinate section, capped at 5 lines, with a moat-honest CTA.
  4. Ship's ponytail line is NOT enough: formalized as delegate-when-present with a degrade path; sdd delegation is weight-scoped and surfaces the choice.
- anchors.plan now points at the PLAN doc (phased source of truth for the build); the spec is in anchors.files.
- STATUS stays `parked` on purpose: the CODE build (plan Tasks 1-5 = Layer 2 + Layer 3 + ponytail) is fully planned but NOT started, and remains demand-gated on real dual-user demand (the item's SEQUENCING decision). This session deliberately planned rather than built.
- To build when demand is real: `/throughline ship 0002`, reconstruct from the plan doc, execute Tasks 1-5 (Layer 2 is TDD'd code; Layer 3 + ponytail are skill-text edits). The plan's byte-identical standalone-first regression test is the guard that core behavior does not change.
- Session order resumes at item 0001 (Phase 4: publish, only on explicit user go-ahead).
- After committing this session's work (README + spec + plan + this item), re-baseline this item's anchor sha so drift-check reads clean.

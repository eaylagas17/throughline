---
id: 0001
title: "Plan B: adoption layer (README, benchmark, examples, LICENSE)"
status: in-progress
intent: "Plan A (core plugin) is done and merged, but the repo is bare. Adoption is what earns the ponytail/impeccable-tier reach we're aiming for, and that lives in the README's sticky one-liner + honest lineage + a quotable, honest proof number — not in the code. Publishing before this wastes the one-shot first impression."
decisions:
  - "Do Plan B BEFORE publishing to GitHub — a bare repo undersells, and publishing is outward-facing and effectively permanent."
  - "Name is throughline; one-liner 'Keep the throughline across every session'; hero demo = drift-proof phase handoffs."
  - "README lineage must be HONEST: Task Master / Backlog.md store tasks, superpowers persists phases; throughline adds capture-fidelity + startup-surfacing + anti-drift handoff. Do not claim to be first."
  - "Reuse ponytail (minimal writing) + superpowers plan format; zero runtime dependencies stays a selling point."
  - "RESOLVED 2026-07-04: publish account = eaylagas17 (github.com/eaylagas17); MIT confirmed against package.json. Author NAME stays 'Enrique Aylagas'."
  - "STYLE (2026-07-04, user directive): NO em dashes in reader/contributor-facing text (README, AGENTS.md, CLAUDE.md, references, skills, commands, source comments, tests); they read as AI-generated. Replace context-aware (comma/colon/parens), never a bare spaced hyphen. Surface + drift OUTPUT separators are a middot '·' (render.mjs, drift-check.mjs). Quote phased-item names containing a colon ('Phase N: desc'). docs/superpowers/ specs were intentionally left dashed (out of scope). Do NOT reintroduce em dashes when writing examples/ (Phase 2) or the benchmark (Phase 3)."
  - "RESOLVED 2026-07-04 (Phase 3, open_question): proof metric = a 3-item plan-adherence checklist (client drops Bearer; CSRF added; server Bearer path KEPT), scored from each run's stated edits; headline = drift rate on the 'kept the Bearer path' item. Baseline = a plausible prose paste-prompt with no plan pointer; treatment = the throughline item + Ship re-validate. Form = zero-dep reproducible RUNBOOK (user-approved over an API auto-scorer, to protect the zero-dep story). Result recorded as a subagent proxy, N=10/arm over two runs (double-checked 2026-07-04): prose drifted 9/10 (one baseline kept the path unaided, the honest counter-case), throughline 0/10. Method + limits in benchmarks/README.md."
open_questions: []
acceptance: "README (one-liner + honest lineage + 30s install + badges), a reproducible benchmarks/ demo proving acceptance-scenario #2 with method+limits stated, examples/ (parked-item + phase-handoff before/afters), and a LICENSE file all exist; the repo reads as ready to publish."
anchors:
  sha: 6ae803ca3b190caf94932e04c3c6a60f16fd74cc
  files:
    - docs/superpowers/specs/2026-07-03-throughline-design.md
    - docs/superpowers/plans/2026-07-03-throughline-core-plugin.md
    - README.md
  plan: docs/superpowers/specs/2026-07-03-throughline-design.md
phases:
  - name: "Phase 1: README + LICENSE file"
    status: done
    delta: |
      Shipped README.md + LICENSE (MIT, © 2026 Enrique Aylagas). Decisions a fresh
      session must respect (not re-derivable from the diff):
      - HONESTY over polish: cited the REAL test count (67, via `npm test`), not the
        stale "45/45"/"47/47" in the item body & CLAUDE.md. Those docs are now stale —
        a minor to fix later, out of Phase 1 scope.
      - Badges are only always-true facts (MIT, Node≥20, zero-deps, plugin host).
        Deliberately NO CI/tests-passing badge — CI isn't wired yet; a broken/stale
        badge would undercut trust. Add a real CI badge when CI lands (Phase 4 / plan
        item 4).
      - Did NOT fabricate a benchmark proof number. README describes the mechanism only;
        the quotable, honest number waits for Phase 3's benchmark. Keep it that way.
      - GitHub handle CORRECTED to `eaylagas17` (user-confirmed 2026-07-04). Plan A's
        manifests had hardcoded the wrong handle `enriqueaylagas`; fixed in README +
        .claude-plugin/plugin.json + marketplace.json. The historical plan doc
        (docs/superpowers/plans/...) still shows the old handle — left as a dated
        artifact, not rewritten. The Codex `plugin marketplace add` form came from design
        spec §8 — RE-VERIFY exact Codex CLI syntax before publish (Phase 4).
      - README restructured to ponytail's proven shape (user asked to model it on
        DietrichGebert/ponytail): narrative drift-scene hook, inline before/after +
        pointer to examples/, short FAQ. RESERVED a "Numbers" section for the top of the
        README — fill it with Phase 3's real benchmark figure (do NOT fake it before then).
      - Lineage stated honestly per the decision: Task Master/Backlog.md store tasks;
        superpowers persists phases; throughline adds capture-fidelity + surfacing +
        anti-drift. Explicitly "not the first backlog tool."
  - name: "Phase 2: examples/ (parked-item + phase-handoff before/afters)"
    status: done
    delta: |
      Shipped examples/ at the repo root. Decisions a fresh Phase 3 session must respect
      (not re-derivable from the diff):
      - STRUCTURE: examples/README.md (index) + two folders. Each folder has before.md
        (the lossy artifact), after.md (the .throughline item), and its own README.md
        (walkthrough). phase-handoff/ ALSO ships plan.md, on purpose: the hero demo only
        works if re-validate has a real source-of-truth file to check against.
      - FRESH scenarios by design: parked-idea uses API rate-limiting; phase-handoff uses
        a JWT-localStorage to httpOnly-cookie migration. Deliberately NOT the README's
        dark-mode/SSR snippet, so examples/ adds NEW worked material (the README says
        "more worked before/afters live in examples/"). Phase 3's benchmark should not
        just re-run dark-mode either.
      - The phase-handoff planted drift is concrete and honest: plan.md says the server's
        Authorization: Bearer path stays until Phase 3; the prose paste-prompt says delete
        it in Phase 2. re-validate catches exactly that. This is a ready-made, honest hero
        scenario Phase 3's benchmark can reuse or adapt for the quantitative score.
      - Did NOT fabricate any benchmark number (same rule as Phase 1). examples/README.md
        explicitly defers the quantitative proof to the forthcoming benchmark. Keep it that.
      - Fixed the README's previously-dangling ./examples link (it now resolves).
      - STYLE honored: zero em/en dashes across all 8 files (verified by grep). Tests still
        67/67 green (examples/ is docs-only, no code touched).
  - name: "Phase 3: handoff-fidelity benchmark (the hero proof; resolve the proof-metric question)"
    status: done
    delta: |
      Shipped benchmarks/. Decisions a fresh Phase 4 session must respect (not
      re-derivable from the diff):
      - FORM: a zero-dep reproducible RUNBOOK (fixed scenario + rubric + prompts +
        results), chosen over an API auto-scorer to protect the zero-dependency story
        (user-approved 2026-07-04). No script, no API key.
      - RUBRIC: 3-item plan-adherence checklist; headline = drift rate on "kept the
        server Bearer path." See benchmarks/rubric.md.
      - SCENARIO reuses examples/phase-handoff (cookie migration + the planted
        Bearer-path drift). The post-Phase-1 code fixture is embedded INLINE in both
        prompt files so each run is hermetic: the baseline arm literally cannot read the
        plan, which is the whole point of the baseline.
      - RESULT is a subagent-proxy trial, N=10/arm pooled over TWO runs (the second was
        a double-check): baseline (prose) drifted 9/10; throughline (item + re-validate)
        drifted 0/10, and every throughline run's re-validate step NAMED the temptation
        and refused it. One baseline run kept the path unaided (reasoned about rollout
        risk) = the honest counter-case. Raw per-run log in benchmarks/results.md.
      - HONESTY: the number is labeled a proxy (not full CC sessions with the hook),
        small N, single scenario, same model both arms. The 9/10 vs 0/10 is flagged as
        scenario-specific (one sharp planted drift); real-world separation will be
        messier. Did NOT dress it up (run 1 alone was a suspiciously clean 5/5 vs 0/5;
        the double-check pooled it to the more credible 9/10 vs 0/10).
      - README: added a "Does it hold up?" section AFTER the before/after (NOT at the very
        top, where Phase 1 had reserved a "Numbers" slot). Deliberate editorial call so a
        caveated proxy figure does not oversell as the first thing a reader sees. This
        supersedes Phase 1's "fill the top Numbers section" note.
      - STYLE honored: zero em/en dashes across benchmarks/ + the README edit (grep-
        verified). Tests still 67/67 (docs-only, no code touched).
  - name: "Phase 4: publish to GitHub (only after explicit user go-ahead)"
    status: pending
---
Captured at the end of the build session (2026-07-03). Plan A is merged to main @ 005f61e, 45/45 tests green.
Source of truth for this work: the design spec §8b (adoption deliverables), §11 (acceptance #2), §12 (scope), §13
(open decisions), plus the "Plan B" section at the end of the core-plugin plan.
Fresh-session order: read this repo's CLAUDE.md, then this item, then the design spec's Plan B
sections (§8b/§11/§12/§13) via the `plan:` anchor, then write a Plan B plan.
Remaining non-blocking Minors from the build are in .superpowers/sdd/progress.md (git-ignored).

---
Checkpoint 2026-07-04 (after Phase 1):
- Phase 1 (README + LICENSE) is DONE and committed locally: 4 commits on `main`, nothing pushed (see `git log`). Publishing stays Phase 4, gated on user go-ahead + Plan B complete (decision above).
- The phase breakdown below IS the plan. Resume directly with `/throughline ship 0001` at Phase 2. (This supersedes the "write a Plan B plan" line above: the design spec §8b plus these phases are the plan; no separate plan doc was needed for README/LICENSE.)
- The STYLE decision above now applies to ALL remaining phases. Do not reintroduce em dashes in examples/ or the benchmark.
- Phase 2 = create `examples/`. The README already links `./examples`, so building it also fixes that currently-dangling link before publish.
- Real test count is 67 (the "45/45"/"47/47" figures above and in CLAUDE.md are stale).

---
Checkpoint 2026-07-04 (after Phase 2):
- Phase 2 (examples/) is DONE, not yet committed (working tree: new `examples/` + this item). Committing stays the user's call; publishing is still Phase 4, gated on go-ahead.
- Drift anchor re-baselined to f14a133 (post-Phase-1 HEAD), so the benign "README changed" stale flag is cleared. examples/ is this item's own output, not an anchor input, so it's not listed under anchors.files.
- Resume with `/throughline ship 0001` at Phase 3: the handoff-fidelity benchmark. This is where the open_question (plan-adherence rubric + a fair paste-prompt baseline) must finally be resolved with the user. The cookie-migration scenario in examples/phase-handoff/ (plan.md + the planted Bearer-path drift) is a ready-made, honest case the benchmark can reuse.
- The reserved "Numbers" section at the top of the README waits for Phase 3's real figure. Do NOT fake it.

---
Checkpoint 2026-07-04 (after Phase 3):
- Phase 3 (benchmarks/) is DONE, NOT yet committed. Working tree on top of a613ae0: new `benchmarks/`, the README "Does it hold up?" section, and this item. User asked to ship Phase 3, not commit it; committing is their call.
- The single open_question is RESOLVED (proof metric decided, first honest proxy result recorded). Nothing on this item is open now except Phase 4.
- Only Phase 4 remains: publish to GitHub, ONLY after explicit user go-ahead. Pre-publish checklist: re-verify the Codex `plugin marketplace add` syntax (design spec §8); wire CI and add a real CI badge (plan item 4); fix the stale "45/45"/"47/47" test counts in CLAUDE.md and this item's older notes (real = 67); and AFTER committing Phase 3, re-baseline this item's anchor sha so drift-check reads clean (README will otherwise flag as changed vs f14a133).

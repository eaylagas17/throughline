node_modules/
*.log
.DS_Store

# Local-only build ledger (progress + review diffs); never publish.
.superpowers/

# Design artifacts (specs + plans) are maintained locally, not published.
docs/superpowers/
